#!/bin/sh
# fix /etc/group and /etc/gshadow desync caused by hhd's rechunker. thanks @tulilirockz!
sed --sandbox -i -e "$(sed -En -e '/wheel|root|sudo/d' -e 's@^g\s+(\S+)\s.*@/\1/d@p' /usr/lib/sysusers.d/*.conf)" "$1"
